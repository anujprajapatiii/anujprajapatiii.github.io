---
import PageLayout from "@/layouts/PageLayout.astro";
import EmbedFrame from "@/components/EmbedFrame.astro";
import { getCollection, render } from "astro:content";

export async function getStaticPaths() {
  const projects = await getCollection("projects", ({ data }) => !data.draft);
  return projects.map((project) => ({
    params: { slug: project.id },
    props: { project },
  }));
}

const { project } = Astro.props;
const { Content } = await render(project);
---

<PageLayout
  title={`${project.data.title} — Anuj Prajapati`}
  description={project.data.description}
  image={project.data.heroImage ?? project.data.thumbnail}
>
  <article class="pt-12 pb-24 sm:pt-20">
    <header class="mx-auto mb-12 max-w-content px-6">
      <a
        href="/work"
        class="text-small text-muted-foreground transition-colors hover:text-foreground"
      >&larr; Work</a>
      <h1 class="mt-6 text-h1">
        {project.data.title}
      </h1>
      <p class="mt-4 max-w-narrow text-lead text-muted-foreground">
        {project.data.description}
      </p>
      <dl class="mt-8 flex flex-wrap gap-x-10 gap-y-4 border-t border-border pt-6">
        {project.data.role && (
          <div>
            <dt class="label">Role</dt>
            <dd class="mt-1 text-small">{project.data.role}</dd>
          </div>
        )}
        {project.data.year && (
          <div>
            <dt class="label">Year</dt>
            <dd class="mt-1 text-small">{project.data.year}</dd>
          </div>
        )}
        {project.data.skills.length > 0 && (
          <div>
            <dt class="label">Skills</dt>
            <dd class="mt-1 text-small">{project.data.skills.join(", ")}</dd>
          </div>
        )}
      </dl>
      {project.data.liveUrl && (
        <a
          href={project.data.liveUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="mt-8 inline-flex items-center gap-2 rounded-md border border-border px-4 py-2 text-small transition-colors hover:border-foreground/40 hover:text-accent"
        >
          Try it live
          <span aria-hidden="true">&rarr;</span>
        </a>
      )}
    </header>

    {project.data.embedUrl && (
      <EmbedFrame url={project.data.embedUrl} title={project.data.title} />
    )}

    <div class="mx-auto max-w-content px-6">
      {project.data.heroImage && (
        <img
          src={project.data.heroImage}
          alt=""
          class="mb-12 w-full rounded-xl border border-border"
        />
      )}

      <div class="prose">
        <Content />
      </div>
    </div>
  </article>
</PageLayout>
