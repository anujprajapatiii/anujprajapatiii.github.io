---
/* Horizontal flex with wrap. See global.css "Layout primitives". */
interface Props {
  gap?: "sm" | "md" | "lg";
  align?: "start" | "center" | "baseline";
  justify?: "start" | "between";
  as?: string;
  class?: string;
}
const {
  gap = "md",
  align = "center",
  justify = "start",
  as = "div",
  class: className,
} = Astro.props;
const Tag = as as any;
---

<Tag
  class:list={[
    "cluster",
    `cluster--gap-${gap}`,
    align !== "center" && `cluster--align-${align}`,
    justify === "between" && "cluster--justify-between",
    className,
  ]}
>
  <slot />
</Tag>
