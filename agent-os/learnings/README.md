# Learnings

Reusable project memory extracted from completed work. One file per topic
(e.g. `deployment.md`, `content-migration.md`), written so a future session
can avoid re-discovering the same gotcha.

A good learning note states the gotcha, why it happens, and what to do
instead. Skip anything already covered by a convention file — update the
convention instead.
